# PostPilot Rules

## 项目范围

- 项目使用 Electron, React, Vite, TypeScript, Node 24 和本地 SQLite。
- 模型固定为 `deepseek-v4-flash`, 不在界面中提供模型切换。
- 不主动修改 `.env`, 不把密钥写入仓库。
- 用户可在应用设置页维护平台账号配置。

## 代码与界面

- 用户可见文案, 错误信息和状态提示使用中文。
- 主界面保持简洁, 不展示无关说明文字。
- 草稿默认需要手动审核, 审核通过后才允许真实发布。
- 真实发布失败必须记录明确中文回执, 不允许把失败或未接入伪装成成功。
- 新平台应通过平台适配器, 账号 schema 和官方连接器扩展, 不在界面逻辑中硬编码特殊流程。

## PR 与依赖

- PR 描述不能空白, 也不能与实际代码变更严重不符。
- 新增第三方库或框架时, 必须在 README 中列明依赖。
- PR 描述中必须说明原创功能部分。
- 如果复用外部代码或过去项目代码片段, 必须注明来源和修改范围。

## Git 工作流

- 一个相对独立模块完成后提交并推送。
- commit message 使用中文, 简洁说明完成内容。
- 不强制 push, 不删除用户未确认的分支或文件。
